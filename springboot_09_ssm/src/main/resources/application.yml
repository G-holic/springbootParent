# TODO 配置数据源相关信息

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ssm_db
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource # 注意数据源不要配错

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 80